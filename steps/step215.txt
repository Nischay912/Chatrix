Commit all changes now in a new branch by : ctrl+shift+p > create new branch > update-profile > go on sidebar git icon > click on + next to changes written there > stage the changes > commit with a message there > then goon github and merge the pull request > then come back here and form bottom left choose the master branch and then sync all changes using the refresh or sync button in the bottom let there too now > thus all changes will be synced from the update-profile branch in the master main branch on our local vs code too now.

See the next steps now in the step216.txt file now there.